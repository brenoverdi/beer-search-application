# Set this to your deployed API URL in Vercel project settings instead.
# Vercel injects VITE_API_BASE as a build-time env var from the dashboard.
# This file is a placeholder — the real value comes from Vercel environment variables.
VITE_API_BASE=https://your-api-project.vercel.app/api
